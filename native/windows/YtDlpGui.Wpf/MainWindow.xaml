<Window x:Class="YtDlpGui.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="yt-dlp GUI Native"
        Width="1120"
        Height="760"
        MinWidth="980"
        MinHeight="700"
        WindowStartupLocation="CenterScreen"
        Background="#F3F6FB"
        FontFamily="Segoe UI"
        Foreground="#19202C"
        Loaded="Window_Loaded">
    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="SurfaceAltBrush" Color="#F8FAFE" />
        <SolidColorBrush x:Key="BorderBrush" Color="#D9E1ED" />
        <SolidColorBrush x:Key="TextBrush" Color="#19202C" />
        <SolidColorBrush x:Key="MutedBrush" Color="#66758B" />
        <SolidColorBrush x:Key="AccentBrush" Color="#1463D6" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#1158BF" />

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Padding" Value="14" />
        </Style>

        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="MinWidth" Value="120" />
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.94" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.82" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.56" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="MinHeight" Value="38" />
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,0,18,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="8" />
            <Setter Property="Background" Value="#E5EAF2" />
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="4" />
                            <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" CornerRadius="4" HorizontalAlignment="Left" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListView">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style x:Key="SectionLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock FontSize="30" FontWeight="SemiBold" Text="yt-dlp GUI Native" />
                        <TextBlock Foreground="{StaticResource MutedBrush}" Text="Native desktop downloader for Windows" />
                    </StackPanel>

                    <Button x:Name="CheckUpdatesButton"
                            Grid.Column="1"
                            Margin="0,0,8,0"
                            Style="{StaticResource BaseButtonStyle}"
                            Click="CheckUpdatesButton_Click"
                            Content="Check updates" />

                    <Button x:Name="UpdateButton"
                            Grid.Column="2"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="UpdateButton_Click"
                            Content="Install yt-dlp" />
                </Grid>

                <WrapPanel Margin="0,12,0,0">
                    <Border Background="#EEF2F8" CornerRadius="10" Padding="10,5" Margin="0,0,8,0">
                        <TextBlock x:Name="BinaryStateText" Foreground="#415166" Text="yt-dlp unknown" />
                    </Border>
                    <Border Background="#EEF2F8" CornerRadius="10" Padding="10,5">
                        <TextBlock x:Name="UpdateStateText" Foreground="#415166" Text="Update status unknown" />
                    </Border>
                </WrapPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Style="{StaticResource SectionLabelStyle}" Text="Video / Playlist URL" />
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="UrlInput" Grid.Column="0" Margin="0,0,8,0" KeyDown="UrlInput_KeyDown" />
                    <Button Grid.Column="1"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="AddJobButton_Click"
                            Content="Add to Queue" />
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Style="{StaticResource SectionLabelStyle}" Text="Format" />
                        <ComboBox x:Name="FormatCombo" SelectionChanged="FormatCombo_SelectionChanged" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Style="{StaticResource SectionLabelStyle}" Text="Quality" />
                        <ComboBox x:Name="QualityCombo" />
                    </StackPanel>
                </Grid>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Style="{StaticResource SectionLabelStyle}" Text="Save to" />
                    <TextBox x:Name="OutputDirInput" />
                </StackPanel>

                <WrapPanel>
                    <CheckBox x:Name="SubtitlesCheck" Content="Subtitles" />
                    <CheckBox x:Name="SponsorBlockCheck" Content="SponsorBlock" />
                    <CheckBox x:Name="PlaylistCheck" Content="Full Playlist" Margin="0" />
                </WrapPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" FontSize="22" FontWeight="SemiBold" Text="Queue" VerticalAlignment="Center" />
            <Button Grid.Column="1"
                    Margin="0,0,8,0"
                    Style="{StaticResource BaseButtonStyle}"
                    Click="StartAllButton_Click"
                    Content="Download All" />
            <Button Grid.Column="2"
                    Style="{StaticResource BaseButtonStyle}"
                    Click="ClearDoneButton_Click"
                    Content="Clear Done" />
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Padding="10,12,10,10">
            <ListView x:Name="JobsList" ItemsSource="{Binding Jobs}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="12" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}" Padding="10">
                            <StackPanel>
                                <Grid Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Column="1" Text="{Binding StatusLabel}" Foreground="{StaticResource MutedBrush}" />
                                </Grid>

                                <ProgressBar Minimum="0" Maximum="100" Value="{Binding Progress}" Margin="0,0,0,8" />

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding DetailsLabel}"
                                               Foreground="{StaticResource MutedBrush}"
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center" />
                                    <Button Grid.Column="1" Margin="8,0,0,0" MinWidth="72" Style="{StaticResource BaseButtonStyle}" Content="Start" Tag="{Binding}" Click="StartJobButton_Click" />
                                    <Button Grid.Column="2" Margin="8,0,0,0" MinWidth="72" Style="{StaticResource BaseButtonStyle}" Content="Cancel" Tag="{Binding}" Click="CancelJobButton_Click" />
                                    <Button Grid.Column="3" Margin="8,0,0,0" MinWidth="72" Style="{StaticResource BaseButtonStyle}" Content="Open" Tag="{Binding}" Click="OpenFolderButton_Click" />
                                    <Button Grid.Column="4" Margin="8,0,0,0" MinWidth="78" Style="{StaticResource BaseButtonStyle}" Content="Remove" Tag="{Binding}" Click="RemoveJobButton_Click" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <TextBlock x:Name="FooterStatsText"
                   Grid.Row="4"
                   Margin="2,8,0,0"
                   Foreground="{StaticResource MutedBrush}"
                   Text="Ready" />
    </Grid>
</Window>
